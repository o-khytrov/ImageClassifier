<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">IEI технологія</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Головна
            </NavLink>
            <NavLink class="nav-link" href="@($"{Base}/TrainingDetails")">
                <span class="oi oi-list" aria-hidden="true"></span> Деталі тренування
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;


#if DEBUG
    private string Base = "";
#else
private string Base = "/ImageClassifier";
#endif

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

}